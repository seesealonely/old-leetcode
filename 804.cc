/*
*/

#include"804.h"

int main()
{
	vector<string> v;
	v.push_back("gin");
	v.push_back("zen");
	v.push_back("gig");
	v.push_back("msg");
	Solution s;
	cout<<s.uniqueMorseRepresentations(v)<<endl;
	return 0;
}
